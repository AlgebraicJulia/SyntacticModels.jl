
@sum Var begin
  Untyped(var::Symbol)
  Typed(var::Symbol, type::Symbol)
end

@sum UWDTerm begin
  Statement(relation::Symbol, variables::Vector{Var})
  UWDExpr(context::Vector{Var}, statements::Vector{UWDTerm})
  UWDModel(header::amr.Header, uwd::UWDTerm)
end